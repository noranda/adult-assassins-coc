<nav class="navbar navbar-inverse navbar-static-top" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-header-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{#link-to 'index' class='navbar-brand navbar-logo'}}<img src="/assets/images/logo-dark-sm.png" alt="Adult Assassins" />{{/link-to}}
    </div>
    <div class="collapse navbar-collapse" id="navbar-header-collapse">
      <ul class="nav navbar-nav navbar-right">
        {{#if isAuthenticated}}
          {{#active-li}}
            {{#link-to 'dashboard'}}
              <i class="fa fa-tachometer fa-fw"></i>
              Dashboard
            {{/link-to}}
          {{/active-li}}
          {{#active-li}}
            {{#link-to 'chatroom'}}
              <i class="fa fa-comments fa-fw"></i>
              Chat Room
            {{/link-to}}
          {{/active-li}}
          {{#active-li}}
            {{#link-to 'rules'}}
              <i class="fa fa-list-ol fa-fw"></i>
              Clan Rules
            {{/link-to}}
          {{/active-li}}
          <li>
            <a href="#" {{action 'logout'}}>
              <i class="fa fa-sign-out fa-fw"></i>
              Log Out
            </a>
          </li>
        {{else}}
          {{#active-li}}
            {{#link-to 'login'}}Log In{{/link-to}}
          {{/active-li}}
          {{#active-li}}
            {{#link-to 'register'}}Register{{/link-to}}
          {{/active-li}}
        {{/if}}
      </ul>
    </div>
  </div>
</nav>
